# Local development can run with DEBUG=True and this sample key.
# Production must set DEBUG=False and provide a strong unique SECRET_KEY.
DEBUG=True
SECRET_KEY=change-me-to-a-long-random-secret

DB_NAME=erp_db
DB_USER=postgres
DB_PASSWORD=your-db-password
DB_HOST=localhost
DB_PORT=5432

ALLOWED_HOSTS=127.0.0.1,localhost
APP_ENV=Development

# Frontend/API browser access.
# In production, set explicit HTTPS origins and keep CORS_ALLOW_ALL_ORIGINS=False.
CORS_ALLOWED_ORIGINS=http://localhost:5173
CORS_ALLOW_CREDENTIALS=True
CORS_ALLOW_ALL_ORIGINS=False
CSRF_TRUSTED_ORIGINS=http://localhost:5173

# Production security. Keep these local defaults for development only.
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False
USE_X_FORWARDED_PROTO=False
X_FRAME_OPTIONS=DENY
SECURE_CONTENT_TYPE_NOSNIFF=True
